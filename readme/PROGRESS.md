# KeiPix Progress

## 2026-05-25

| Area | Progress | Evidence | Notes |
| --- | ---: | --- | --- |
| Discovery dashboard | 96% | Added a default native Discover route, grouped all non-home sidebar destinations into SwiftUI dashboard sections, surfaced quick local counts for saved searches, history, pinned creators, manga watchlist, downloads, and Spotlight library state, and added route coverage tests plus `discover-dashboard` visual QA coverage. `swift test` passed with 46 tests; `./script/capture_visual_qa.sh discover-dashboard` captured `20260525T123334Z`. | 已消失：首页发现入口信息密度不足、路由散落、以及后续新增板块容易漏接入口的风险。仍存在：后续新增小说或媒体库时需要同步扩展 dashboard coverage。下一优先级：新增板块时同步刷新首页视觉 QA。 |
| Gallery selection and batch actions | 97% | Added native gallery selection state, Command-click/selection-mode card toggling, visible checkmark overlays, selected-work menu actions for copying Pixiv links, batch downloads, batch bookmark preview, and bulk mute preview. Added SwiftPM coverage for selection pruning and lifecycle. | 已消失：批量收藏只能作用于已加载全部作品、无法对选中子集下载/收藏/复制链接、以及切换路由后选择残留的风险。仍存在：后续可以把多选状态接入更完整的菜单命令。下一优先级：刷新 gallery visual QA。 |
| Safety and filtering | 97% | Added optional R-18/R-18G preview masking in Settings and the toolbar App Controls menu, persisted through `UserDefaults`, and applied it across main gallery cards, masonry cards, hot tags, search popular preview, creator preview thumbnails, recent works, related works, browsing history, and series grids. Added feed-level bulk mute previews for loaded artworks, creators, and top visible tags with affected-work counts and one-step undo through a muted-content snapshot. `swift build`, `swift test` with 17 tests, and `./script/build_and_run.sh --verify` passed; Computer Use verified masked R-18 ranking cards and the visible-creator bulk mute preview popover without applying destructive changes. | 已消失：成人内容缩略图直接暴露、截图分享缺少二级保护、热门标签/作者预览漏掉敏感缩略图遮罩、以及可见作品流缺少批量屏蔽预览的风险。仍存在：远端 Pixiv 屏蔽同步仍需明确授权。下一优先级：保留本地预览优先，远端同步继续走 Settings 的显式确认。 |
| Non-novel QA matrix | 98% | Added Settings → Runtime Readiness non-novel QA matrix with P0/P1/P2 grouped rows, copyable diagnostics text, localized Chinese/English strings, and SwiftPM coverage. Visual QA evidence is now read from window-scoped screenshot manifests and gates P0 gallery, trending tags, and Pixivision rows. `swift build`, `swift test`, and `./script/build_and_run.sh --verify` passed; Computer Use verified the running app after relaunch and a signed-in Settings run returned P0 4/4, P1 8/8, P2 8/8. | 已消失：非小说缺口只能靠文档静态追踪、旧 TLS 问题被误判为当前失败、Settings 窄窗口矩阵横向拥挤、以及 P0 视觉证据只停留在手工记录里的风险。仍存在：下载/阅读器关键路径可以继续补截图回归。下一优先级：对 per-artwork reader resume 和 auto-bookmark-after-download 做下一轮落地。 |
| Account health | 97% | Added a visible Runtime Readiness account-health row plus a one-click Account Health Check that explicitly refreshes the saved Pixiv token, updates the active session and stored account list, and writes the result into copied/saved diagnostics. `swift build` and `swift test` with 32 tests passed. | 已消失：只能通过隐式 401 重试推断 token refresh 是否正常、登录态健康缺少显式按钮验收的风险。仍存在：后续 auth 改动后需要重新运行账号健康检查。下一优先级：Premium 搜索实号证据或作者集合。 |
| Pixiv URL routing | 97% | Added main-window drag/drop for Pixiv/Pixivision links, shared text extraction for dropped snippets, `.webloc` and `.url` document parsing, and packaged URL document metadata while keeping `keipix://`, `pixiv://`, clipboard open, ID quick open, and article links on the same native resolver. `swift test` passed with 34 tests, including dropped-text and web-location file coverage. | 已消失：从浏览器或 Finder 拖入 Pixiv 链接仍可能跳出到 Web、URL/文档入口和 App 内链接解析分散的风险。仍存在：需要一次真实拖放视觉/交互验收并刷新 QA 证据。下一优先级：作者集合或离线作品流快照。 |
| Sharing and copy templates | 96% | Added Settings → Sharing with artwork and creator copy templates, live previews, reset actions, persisted `UserDefaults` values, and shared renderers used by Copy Artwork Summary and Copy Visible Creator Summary. Templates cover IDs, creator account, page/stat counts, tags, Pixiv URL, and AI/R18 markers. `swift test` passed with 36 tests, including copy template rendering coverage. | 已消失：复制摘要格式固定、作者摘要只能是硬编码 TSV、以及 AI/R18/tag 等关键信息无法按用户截图或分享习惯调整的风险。仍存在：可以在后续加入模板导入导出或更多单项作者复制入口。下一优先级：作者集合或离线作品流快照。 |
| Creator workflows | 96% | Added a local pinned creator collection backed by `UserDefaults`, a sidebar Library route, creator-card and profile-menu pin/unpin actions, follow-state synchronization for pinned entries, localized empty states, and model tests. `swift test` passed with 39 tests. | 已消失：作者工作流只能依赖 Pixiv 关注关系、常看作者缺少本地快速集合、推荐/搜索作者无法沉淀为 macOS 侧边栏集合的风险。仍存在：可继续加入集合分组或导入导出。下一优先级：作品流快照缓存。 |
| Local cache and offline | 94% | Added bounded feed snapshot persistence keyed by route, focused creator, search text, bookmark tag, ranking date, and search filters. Successful route reloads and load-more pages update the cache; transient reload failures restore the last successful gallery with a localized cached-feed message. `swift test` covers eviction and JSON round-tripping. | 已消失：网络短暂失败后作品流直接清空、最近一次成功路由无法离线回看、以及加载更多后的缓存停留在第一页的风险。仍存在：可以继续把离线恢复状态做成更明确的只读视觉提示。下一优先级：漫画卡片到系列/追更的入口覆盖。 |
| Manga feeds and watchlist | 95% | Added manga watchlist update/read-state tracking backed by local baselines, optional Pixiv API unread fields, mark-read actions, and latest-work routing that clears the badge after a successful native open. `swift build` and `swift test` with 31 tests passed, including dedicated watchlist baseline/update/API-unread tests. | 已消失：漫画追更列表只能显示静态系列卡片、用户无法区分已读和更新系列、以及 API 未读字段缺少 UI 承接的风险。仍存在：更多漫画入口可以继续增强跳转到系列/追更上下文。下一优先级：Settings 分组与搜索。 |
| Manga card routing | 96% | Added gallery-card context actions for works with Pixiv series metadata: open the series in a native KeiPix sheet, add the series to manga watchlist, open the series in Pixiv explicitly, and copy the series link. `ArtworkSeriesView` now supports a default-expanded sheet mode while keeping the inspector disclosure behavior. | 已消失：漫画作品卡片只能先点作品再慢慢找系列入口、系列操作容易跳 Web、以及追更入口埋在详情页里的风险。仍存在：可以继续给系列 sheet 增加排序/已读筛选。下一优先级：批量收藏冲突显示。 |
| Bookmarking | 95% | Added loaded-feed batch bookmark preview under gallery More Actions. The preview separates unbookmarked candidates from already-bookmarked works, shows the target visibility and shared automatic tags, caps each apply pass, and skips existing bookmarks to avoid overwriting remote tag state. SwiftPM coverage checks preview conflict separation. | 已消失：批量收藏前无法知道会写哪些作品、已收藏作品可能被重复写入并覆盖远端标签、以及批量收藏入口缺少明确确认的风险。仍存在：多选画廊落地后可以把批量收藏范围从已加载作品缩小到选中作品。下一优先级：详情状态恢复。 |
| Artwork detail state | 94% | Added `ArtworkDetailStateLibrary` to persist disclosure state per artwork for description, series, comments, related works, tags, and metadata. The inspector now restores those states when returning to a work while series/comment/related views load through SwiftUI bindings. SwiftPM coverage checks per-artwork scoping and bounded retention. | 已消失：切换作品或回到作品后详情折叠区全部回到默认状态、评论/相关作品入口反复丢失上下文、以及长漫画鉴赏时详情面板需要重复展开的风险。仍存在：评论 stamp 仍需端点验证后实现。下一优先级：刷新 Premium 搜索与远端屏蔽同步证据。 |
| Settings organization | 97% | Reworked Settings into native macOS Form groups for General, Reading, Discovery, Safety, Downloads, Account, and Runtime Readiness, added localized Settings search, and moved the stored account row into its own SwiftUI file to keep `SettingsView.swift` under 1000 lines. `swift build`, `swift test` with 31 tests, and `./script/build_and_run.sh --verify` passed; Computer Use verified the Settings window and a Chinese search for downloads. | 已消失：设置项继续散落、查找下载/阅读/安全选项只能滚动、以及 Settings 单文件超过 1000 行的风险。仍存在：可以继续补下载页与阅读器窗口截图证据。下一优先级：下载/阅读器视觉 QA。 |
| Discovery visual surfaces | 93% | `swift build`, `swift test`, and `./script/build_and_run.sh --verify` passed after adding shared `DiscoveryCardPresentation`, tightening trending-tag masonry spacing, replacing tag-card `GeometryReader` drawing with fixed container fill, and giving Spotlight cards a stable native SwiftUI thumbnail/card model. Captured repeatable visual evidence for `gallery-feed`, `trending-tags`, `pixivision`, and `narrow-window`. | 已消失：热门标签卡片继续堆叠临时 Geometry 修补、Spotlight 卡片尺寸模型分散、Pixivision 文章内 Pixiv 标签/搜索链接缺少原生 resolver 接入、以及窄窗口状态缺少证据的风险。仍存在：可增加下载页与阅读器窗口截图作为 P1 视觉回归。 |
| Visual QA evidence | 97% | Added `download-queue` and `reader-window` visual surfaces to the QA evidence model and matrix checks, changed `script/capture_visual_qa.sh` to capture the frontmost KeiPix window, and captured fresh ignored manifests for both surfaces. `swift build`, `swift test` with 31 tests, and `./script/build_and_run.sh --verify` passed. | 已消失：下载页与阅读窗口只能靠口头视觉验收、阅读窗口会被主窗口面积抢占截图、以及 QA 矩阵无法读取下载/阅读器证据的风险。仍存在：Settings 窗口可在后续纳入截图证据。下一优先级：Ugoira 播放速度和帧导出。 |
| Search | 98% | Added JSON import/export for the saved search library, covering filtered presets, saved keywords, and local history through native macOS open/save panels; `swift build`, `swift test` with 17 tests, and `./script/build_and_run.sh --verify` passed; Computer Use verified the Saved Searches UI exposes import/export beside the library filter. Earlier work added Pixiv Premium male/female popular sorts, 20k/50k/100k bookmark thresholds, non-premium sort fallback, Pixiv Web order mapping, search diagnostics, AI filters, local filtering, saved preset decoding, summaries, and Pixiv Web URLs. | 已消失：搜索过滤能力、热门男女排序、高收藏阈值、保存搜索/预设迁移、回归保护缺口和 Premium 搜索诊断不可见风险。仍存在：Premium popular result behavior requires a Premium account to produce passed diagnostics. 下一优先级：run Settings → Runtime Readiness → Search Diagnostics on a Premium test account. |
| Downloads | 99% | Added native SwiftUI page-range download sheet for multi-page artworks, queue support for arbitrary source page ranges, source-page labels in the download list, and localized English/Chinese strings. Added retry-all-failed backoff scheduling, an explicit Settings toggle for automatically bookmarking newly downloaded artworks, download-record-scoped local viewer resume, and `download-queue` visual evidence. `swift build` and `swift test` with 31 tests passed. | 已消失：50P+ 漫画只能整作或当前页下载、失败项批量重试瞬时压满连接、下载与收藏联动只能单向工作、下载页本地阅读器关闭后丢失页码、以及下载队列缺少视觉证据的风险。仍存在：本地下载阅读器可以继续补独立窗口截图。 |
| Reader | 99% | Added persisted default reading modes for ordinary artworks and manga/long multi-page works, shared by the detail inspector and standalone reader window, with Settings controls and localized copy. Added a standalone reader full-screen focus preset, floating focus controls, a page-jump overlay, per-artwork last-page resume, and downloaded local viewer resume backed by bounded local progress libraries. `swift build` and `swift test` with 26 tests passed. | 已消失：每次打开同类作品都回到固定阅读模式、50P+ 阅读器缺少快速跳页和专注全屏入口、多 P 作品重新打开回到第一页、以及本地下载阅读器不恢复页码的问题。仍存在：继续保留视觉 QA 和触控板回归验收。 |
| Ugoira | 95% | Added playback speed choices for 0.5x/1x/1.5x/2x, current-frame PNG export from the Ugoira player, localized English/Chinese strings, and SwiftPM tests for delay scaling. `swift build`, `swift test` with 32 tests, and `./script/build_and_run.sh --verify` passed. | 已消失：动图只能按原速播放、无法从 App 内快速导出当前帧的风险。仍存在：可在后续媒体库视图里增加封面/海报快捷操作。下一优先级：作者置顶/集合。 |
| AI visibility | 92% | `9e8004a` added Settings copy and Runtime Readiness row clarifying that KeiPix currently handles AI visibility locally and through search `search_ai_type` filters; remote Pixiv account-wide AI display remains outside the verified App API surface | 已消失：AI 显示能力边界不可见风险。仍存在：Pixiv account-wide AI display needs a verified App API endpoint before remote sync can be implemented. 下一优先级：keep local hide-AI and search AI filters as the supported path until an endpoint is verified. |
| Direct Pixiv navigation | 96% | `da812fa` added a native App Controls sheet for opening artwork or creator by numeric Pixiv ID, reusing existing signed-in artwork/user loading paths and adding SwiftPM input parsing tests; `121a70f` added Settings ID navigation diagnostics for the selected artwork and creator; `37dd4a4` added a read-only QA runner that includes ID diagnostics; `ac2b8c2` added search-field quick open prefixes such as `illust:123` and `user:123`; `e67cdd1` surfaced those hints in search and the ID sheet; `swift test` and `swift build` passed | 已消失：只能从 Pixiv URL/clipboard 跳转、缺少 Pixez/Pixes 常见 ID 直达入口、ID 直达缺少可运行验收证据、以及 ID 快捷语法不可发现的风险。仍存在：real account route pass/fail evidence should be captured through Read-only QA. 下一优先级：select an artwork and run Read-only QA. |
| Store maintainability | 94% | Split artwork actions/download/follow helpers into `KeiPixStore+ArtworkActions.swift` and creator/comment/series/list API wrappers into `KeiPixStore+Social.swift`; `KeiPixStore.swift` is now 896 lines; `dfce604` moved download queue actions into `ArtworkDownloadStore+Queue.swift`, leaving `ArtworkDownloadStore.swift` at 600 lines | 已消失：核心 Store 与下载 Store 超 1000 行风险。仍存在：download persistence and file inspection can still split further if export/delete features grow. 下一优先级：keep future download changes in queue, persistence, or file-inspection extensions by domain. |
| SwiftUI view maintainability | 97% | `3e260ea` split Gallery feed header, popular preview, grid, ranking date, and batch download popover; `e9c809d` split creator preview header, status banner, grid, load-more tile, and action menus; `45e60c1` split profile metrics, collection shortcuts, workspace/details, and related creators; `swift build` and `./script/build_and_run.sh --verify` passed | 已消失：`GalleryView.swift`, `UserPreviewListView.swift`, and `UserProfileSheet.swift` large-file maintenance risk. 仍存在：smaller profile header can still move later if follow/feedback grows. 下一优先级：keep new UI changes in dedicated subviews. |
| Signed-in runtime QA | 96% | `./script/build_and_run.sh --verify` launched the app, existing Keychain session loaded, feed/ranking/search/bookmarks/following creators/reader window/download queue/settings diagnostics were navigated through native UI, and network diagnostics passed Pixiv API plus image host checks; `db80f9f`, `d2e489a`, `121a70f`, and `37dd4a4` added search, reversible mutable, ID navigation, comment feedback, and aggregated read-only QA surfaces | 已消失：non-novel signed-in runtime evidence gap for primary routes, P0 QA execution surfaces, ID route checks, comment feedback readiness checks, and read-only QA needing multiple manual button runs. 仍存在：actual pass/fail evidence for Premium search and reversible remote writes depends on a Premium or disposable Pixiv test account. 下一优先级：run Read-only QA plus Reversible Mutable QA after selecting a commented artwork. |
| Mutable action QA readiness | 95% | `953d27b` added `MutableActionQAItem`, `MutableActionQAStatus`, Settings readiness UI, copyable QA checklist, diagnostics text, localization, and SwiftPM tests; `d2e489a` added a typed `TEST ACCOUNT` authorization sheet and reversible private bookmark add/remove plus private follow/unfollow QA runner; `swift test` passed 14 tests | 已消失：可变更动作验收状态不可见风险，以及收藏/关注 P0 缺少可执行测试账号流程的风险。仍存在：comment posting remains explicit-approval manual QA because the current Pixiv API surface has no reversible delete path in KeiPix. 下一优先级：use disposable account diagnostics for bookmark/follow, and keep comment posting as manual explicit-approval QA. |
| Feedback and local mute safety | 94% | `4f81689` added native `FeedbackReportSheet`, Pixiv-compatible safety reasons, report-summary copying, Pixiv Web handoff, and local mute entry points from artwork, comment, and creator menus; `94eb7be` added the profile-header creator feedback entry; `121a70f` added a comment feedback diagnostic; `37dd4a4` added a Settings comment feedback preview that opens the actual feedback sheet for the first loaded comment on the selected artwork; Computer Use verified artwork-detail feedback sheet and profile creator feedback sheet; `swift test`, `swift build`, and `./script/build_and_run.sh --verify` passed | 已消失：作品详情与作者 Profile 缺少安全反馈与屏蔽入口风险、评论反馈缺少可运行检查入口的风险、以及评论反馈 sheet 难以从 Settings 做视觉验收的风险。仍存在：remote report submission remains Pixiv Web handoff. 下一优先级：run Preview Comment Feedback on a commented artwork and save diagnostics. |

## 2026-05-24

| Area | Progress | Evidence | Notes |
| --- | ---: | --- | --- |
| Native macOS route | 82% | SwiftPM app, SwiftUI views, AppKit bridges only for desktop integrations, persistent sidebar toolbar control, and in-app Pixivision article reading via native WebKit bridge | Continue keeping GPL reference projects as behavior references only. |
| Downloads | 90% | Added naming templates, current-page download actions, ugoira ZIP downloads, local ZIP preview, GIF export, queue filters, queue search, sorting, filtered failed-item bulk actions, visible-item bulk management, local storage totals, per-item file sizes, Pixiv link actions, and keyboard/Finder actions in local previews | Supports artwork ID, title, creator, page, extension, AI/R18/R18G, tag placeholders, metadata-backed ugoira items, status/type filtering, sorting, local queue management, downloaded library inspection, copied link sets, first visible reveal, confirmed visible-item deletion, local image reader shortcuts, current-page image sharing/copying/downloading, and ugoira preview shortcuts. |
| Search | 90% | Added autocomplete, local history, saved keywords, saved search presets, saved-search library search, advanced filters, active filter affordances, creator search, tag actions, tag Pixiv Web handoff links, visible result filter summaries, search-summary copying, loaded-result Pixiv link export, per-result Pixiv link copying, Pixiv Web search handoff links, saved-search Web handoff, and one-click filter reset | Saved presets restore keyword plus match type, sort, age, date, bookmarks, work type, and ugoira filters; saved and recent searches can be filtered, copied, promoted to current-filter presets, audited from the result header, recognized from the toolbar, opened in Pixiv Web, exported as Pixiv Web links from the search library, copied as individual or loaded artwork links from the result feed, and artwork detail tags can open or copy their Pixiv Web tag pages. |
| Creator workflows | 90% | Creator search/recommend/following/followers/related pages support local list search, follow/mute filters, native sort menus, resettable creator-list state, creator-list summary chips, confirmed bulk actions, follow visibility badges/checks, copied Pixiv profile links, visible creator link export, visible creator summary export, preview artwork context actions, artwork-detail creator quick actions, related-preview artwork selection, profile header Pixiv actions, profile relationship shortcuts, related-creator context actions, and corrected following/follower semantics | Creator flows now meet the 90% feature target; remaining work is runtime QA evidence across signed-in lists, relationship sheets, artwork-detail follow actions, and bulk actions. |
| Non-novel Pixiv client scope | 91% | Feed, masonry, reader, bookmarks, comments, history, filtering, downloads, muted content, ugoira playback/downloads/export, in-app Pixivision reader, reusable search presets, visible search-state actions, creator list management, artwork summary export, downloaded library management, low-disturbance app packaging verification, and a screenshot-safe runtime readiness diagnostics surface exist | Non-novel feature scope now meets the 90% target; remaining work should focus on signed-in runtime QA passes across feeds, detail, reader, creator lists, downloads, and account-sensitive flows before starting novels. |

Current policy: novels stay deferred until non-novel artwork, manga, discovery, account, download, and reader flows reach at least 90% with build and runtime evidence.
