# KeiPix Progress

## 2026-05-25

| Area | Progress | Evidence | Notes |
| --- | ---: | --- | --- |
| Discovery dashboard | 96% | Added a default native Discover route, grouped all non-home sidebar destinations into SwiftUI dashboard sections, surfaced quick local counts for saved searches, history, pinned creators, manga watchlist, downloads, and Spotlight library state, and added route coverage tests plus `discover-dashboard` visual QA coverage. `swift test` passed with 46 tests; `./script/capture_visual_qa.sh discover-dashboard` captured `20260525T123334Z`. | 已消失：首页发现入口信息密度不足、路由散落、以及后续新增板块容易漏接入口的风险。仍存在：后续新增小说或媒体库时需要同步扩展 dashboard coverage。下一优先级：新增板块时同步刷新首页视觉 QA。 |
| Gallery selection and batch actions | 99% | Added native gallery selection state, Command-click/selection-mode card toggling, visible checkmark overlays, selected-work menu actions for copying Pixiv links, batch downloads, batch bookmark preview, and bulk mute preview. Gallery selection is now exposed through focused macOS commands: `⌘A` selects all visible works, `⇧⌘A` clears selection, and the Artwork menu can copy links, batch-download, or batch-bookmark the selected set through the same preview flow. `swift test` passed with 54 tests after the command wiring. | 已消失：批量收藏只能作用于已加载全部作品、无法对选中子集下载/收藏/复制链接、切换路由后选择残留、以及多选只能靠画廊局部按钮操作的风险。仍存在：远端批量写入继续走显式预览与授权。下一优先级：刷新 gallery visual QA。 |
| QA evidence persistence | 99% | Made non-novel QA matrix snapshots Codable, persisted the latest matrix result in `UserDefaults`, and changed Runtime Readiness to display/copy the last stored matrix across Settings window reopen or app restart. `swift test` passed with 48 tests, including snapshot persistence round-trip coverage; Computer Use verified the latest signed-in selected-artwork matrix persists as P0 4/4, P1 8/8, P2 8/8. | 已消失：非小说 QA 矩阵重开 Settings 后回到未运行、签入验收证据只能存在于临时 `@State`、以及诊断报告无法自动包含最近一次矩阵结果的风险。仍存在：Premium 搜索和远端屏蔽同步仍需要合适账号与明确授权。下一优先级：Premium/远端同步使用合适账号单独验收。 |
| Safety and filtering | 99% | Added optional R-18/R-18G preview masking in Settings and the toolbar App Controls menu, persisted through `UserDefaults`, and applied it across main gallery cards, masonry cards, hot tags, search popular preview, creator preview thumbnails, recent works, related works, browsing history, and series grids. Added feed-level bulk mute previews for loaded artworks, creators, and top visible tags with affected-work counts and one-step undo through a muted-content snapshot. Added `feedback-sheet` visual QA for the native report-summary/local-mute sheet and captured it at `20260525T183312Z`; the Safety/Filtering QA row now requires both `muted-content` and `feedback-sheet`. `swift test` passed with 71 tests. | 已消失：成人内容缩略图直接暴露、截图分享缺少二级保护、热门标签/作者预览漏掉敏感缩略图遮罩、可见作品流缺少批量屏蔽预览、以及反馈/屏蔽 sheet 只能靠口头验收的风险。仍存在：远端 Pixiv 屏蔽同步仍需明确授权。下一优先级：保留本地预览优先，远端同步继续走 Settings 的显式确认。 |
| Non-novel QA matrix | 99% | Added Settings → Runtime Readiness non-novel QA matrix with P0/P1/P2 grouped rows, copyable diagnostics text, localized Chinese/English strings, SwiftPM coverage, persisted last-run snapshots, and visual QA evidence from window-scoped screenshot manifests. Bookmarks now check both public bookmark API readiness and the batch-bookmark preview visual surface. After selecting 2P artwork `#142077008`, Computer Use reran the signed-in matrix at `2026年5月25日 21:52:44`; copied diagnostics report P0 4/4, P1 8/8, P2 8/8. `swift test` passed with 54 tests after adding the new visual surface. | 已消失：非小说缺口只能靠文档静态追踪、旧 TLS 问题被误判为当前失败、Settings 窄窗口矩阵横向拥挤、P0 视觉证据只停留在手工记录、QA 矩阵重开窗口丢失、选中作品后阅读器/详情/评论 QA 跳过、以及批量收藏预览缺少 QA 证据的风险。仍存在：Premium/远端同步需要账号条件。下一优先级：Premium/远端同步使用合适账号单独验收。 |
| Native Apple route | 99% | Added a P0 Runtime Readiness row for the SwiftPM macOS 26 native route and `NativeBoundaryTests` covering Package.swift, SwiftUI-first view files, narrow AppKit bridge files, and absence of Flutter/Dart/Kotlin reference-client implementation paths under `Sources/KeiPix`. `swift test` passed with 70 tests and `L10n.swift` remains at 999 lines. | 已消失：Swift/SwiftUI 原生路线只停留在文档说明、参考 Pixez/Pixes 时缺少源码边界保护、以及 GPL/Flutter/Dart 实现路径混入主源码时没有测试拦截的风险。仍存在：继续参考 Pixez/Pixes 新功能时需要同步维护边界测试。下一优先级：高风险参考迁移前先补测试。 |
| Comment emoji and stamps | 97% | Added a Pixiv comment emoji catalog, parser, SwiftUI emoji text renderer, and native popover picker that inserts Pixiv Web/App-compatible tokens such as `(happy)`. Existing comment stamp image rendering remains in the comment row. `swift test` passed with 50 tests; Computer Use verified the comments disclosure, emoji popover, and token insertion in the signed-in app. | 已消失：Pixez/Pixes 里常见的 Pixiv 评论 emoji token 只能显示为纯文本、用户发评论时缺少快速插入入口、以及评论 stamp/emoji parity 表里长期停留在未落实状态的风险。仍存在：删除自己评论需要可逆端点确认后再做。下一优先级：Premium/远端同步使用合适账号单独验收。 |
| Account health | 98% | Added a visible Runtime Readiness account-health row plus a one-click Account Health Check that explicitly refreshes the saved Pixiv token, updates the active session and stored account list, and writes the result into copied/saved diagnostics. The sidebar avatar now opens account switching for real Keychain accounts, guest preview mode, and local test mode; Settings exposes the same mode picker. Added refresh-token import for Pixez-style migration, which refreshes the token once and stores the resulting Pixiv session in Keychain. `swift build`, `swift test`, and `./script/build_and_run.sh --verify` passed. | 已消失：只能通过隐式 401 重试推断 token refresh 是否正常、登录态健康缺少显式按钮验收、用户必须先 Web 登录才能体验 UI、以及从 Pixez 迁移 refresh token 没有入口的风险。仍存在：Token 导入需要使用可丢弃账号单独实测一次。下一优先级：Premium 完整排序只使用专门测试账号验收，平时使用非 Premium 限量热门预览。 |
| Pixiv URL routing | 99% | Added main-window drag/drop for Pixiv/Pixivision links, shared text extraction for dropped snippets, clipboard text extraction through the same resolver, `.webloc` and `.url` document parsing, and packaged URL document metadata while keeping `keipix://`, `pixiv://`, ID quick open, and article links on one native resolver. Runtime Readiness checks a centralized sample set covering artwork, creator, tag, custom scheme, nested KeiPix URL, and Pixivision article formats, and `./script/build_and_run.sh --visual-qa-pixiv-link-drop` captured `pixiv-link-drop` at `20260525T143805Z`. `swift test` passed with 56 tests. | 已消失：从浏览器或 Finder 拖入 Pixiv 链接仍可能跳出到 Web、剪贴板里带说明文字的 Pixiv 链接无法打开、URL/文档入口和 App 内链接解析分散、QA 只检查少量硬编码链接、以及拖放覆盖层缺少可重复视觉证据的风险。仍存在：真实 Finder/浏览器拖放可在手动 QA 中周期性复核。下一优先级：Premium/远端同步使用合适账号单独验收。 |
| Sharing and copy templates | 98% | Added Settings → Sharing with artwork and creator copy templates, live previews, reset actions, persisted `UserDefaults` values, and shared renderers used by Copy Artwork Summary and Copy Visible Creator Summary. Templates cover IDs, creator account, page/stat counts, tags, Pixiv URL, and AI/R18 markers. Reworked the Settings section into a side-by-side editor/preview layout and added `--visual-qa-sharing-templates`; `sharing-templates` was captured at `20260525T185753Z`. `swift test --filter CopyTemplateTests` passed. | 已消失：复制摘要格式固定、作者摘要只能是硬编码 TSV、AI/R18/tag 等关键信息无法按用户截图或分享习惯调整、以及模板设置缺少可重复视觉证据的风险。仍存在：可以在后续加入模板导入导出或更多单项作者复制入口。下一优先级：保持模板占位符测试和 Settings visual QA 同步。 |
| Creator workflows | 98% | Added a local pinned creator collection backed by `UserDefaults`, a sidebar Library route, creator-card and profile-menu pin/unpin actions, follow-state synchronization for pinned entries, localized empty states, and model tests. Added `--visual-qa-creator-profile`, injected local profile fixtures for metrics/links/workspace/recent works/related creators, and captured `creator-profile` at `20260525T184155Z`; the Creator Discovery QA row now requires that visual surface alongside the live recommended-user API. `swift test` passed with 72 tests. | 已消失：作者工作流只能依赖 Pixiv 关注关系、常看作者缺少本地快速集合、推荐/搜索作者无法沉淀为 macOS 侧边栏集合、作者 Profile 只能靠真实网络手动点开验收、以及推荐作者 QA 缺少独立视觉证据的风险。仍存在：可继续加入集合分组或导入导出。下一优先级：真实账号周期性抽查推荐/关注作者流。 |
| Local cache and offline | 98% | Added bounded feed snapshot persistence keyed by route, focused creator, search text, bookmark tag, ranking date, and search filters. Successful route reloads and load-more pages update the cache; transient reload failures restore the last successful gallery with a persistent native cached-feed strip, read-only load-more behavior, and a live-refresh action. Added a repeatable `--visual-qa-cached-feed` launch mode, corrected cached-feed toolbar status to avoid live load-more wording, and captured `cached-feed` visual evidence at `20260525T151126Z`; Runtime Readiness now requires that surface for the Local Cache row. `swift test` covers eviction, JSON round-tripping, and restoration metadata. | 已消失：网络短暂失败后作品流直接清空、最近一次成功路由无法离线回看、加载更多后的缓存停留在第一页、缓存恢复只靠临时 toast 提示、缓存状态误提示可继续加载、以及离线恢复缺少视觉证据的风险。仍存在：真实断网环境可周期性人工抽查。下一优先级：Premium/远端同步使用合适账号单独验收。 |
| Manga feeds and watchlist | 99% | Added manga watchlist update/read-state tracking backed by local baselines, optional Pixiv API unread fields, mark-read actions, latest-work routing, and series-sheet sort/read filters backed by local browsing history. The app now has repeatable `--visual-qa-manga-watchlist` and `--visual-qa-series-sheet` launch modes; `manga-watchlist` captured `20260525T144749Z`, `series-sheet` captured `20260525T145702Z`, and the QA matrix reads both surfaces for the manga feeds row. `swift test` passed with 56 tests. | 已消失：漫画追更列表只能显示静态系列卡片、用户无法区分已读和更新系列、API 未读字段缺少 UI 承接、长系列 sheet 无法按顺序/时间/本地阅读状态快速定位、追更卡片缺少可重复视觉证据、以及系列 sheet 控件缺少专门截图的风险。仍存在：Premium/远端同步需要合适账号单独验收。 |
| Manga card routing | 99% | Added gallery-card context actions for works with Pixiv series metadata: open the series in a native KeiPix sheet, add the series to manga watchlist, open the series in Pixiv explicitly, and copy the series link. `ArtworkSeriesView` now supports a default-expanded sheet mode, native sort menu, unread/viewed segmented filter, visible count, click-through local history recording, wider series-grid cards to keep stat labels on one line, and a repeatable `series-sheet` visual QA surface captured at `20260525T145702Z`. | 已消失：漫画作品卡片只能先点作品再慢慢找系列入口、系列操作容易跳 Web、追更入口埋在详情页里、系列 sheet 载入几十话后定位困难、以及系列卡片统计数字窄宽度硬换行的风险。仍存在：真实账号的长系列可周期性补人工抽查。下一优先级：Premium/远端同步使用合适账号单独验收。 |
| Bookmarking | 99% | Added loaded-feed and selected-subset batch bookmark previews, explicit scope labels, skipped already-bookmarked samples, automatic shared tag display, capped apply passes, and an Artwork menu command for batch-bookmarking the selected gallery set. SwiftPM coverage checks preview scope, conflict separation, skipped-item sampling, and `./script/capture_visual_qa.sh batch-bookmark-preview` captured `20260525T141518Z`. | 已消失：批量收藏前无法知道会写哪些作品、已收藏作品可能被重复写入并覆盖远端标签、批量收藏只能作用于已加载全部作品、批量收藏入口缺少桌面菜单路径、以及批量收藏预览缺少视觉证据的风险。仍存在：远端批量写入继续走显式预览与授权。下一优先级：在测试账号上单独验收实际批量写入。 |
| Artwork detail state | 98% | Added `ArtworkDetailStateLibrary` to persist disclosure state per artwork for description, series, comments, related works, tags, and metadata. The inspector now restores those states when returning to a work, and initially expanded comments/related sections load through SwiftUI `.task(id:)` instead of waiting for a manual toggle. Added isolated `--visual-qa-artwork-detail-social` with local comments, Pixiv emoji token rendering, stamp preview, feedback/mute controls, related works, and captured `artwork-detail-social` at `20260525T191331Z`. | 已消失：切换作品或回到作品后详情折叠区全部回到默认状态、评论/相关作品入口反复丢失上下文、已展开的评论/相关区首次进入不加载、长漫画鉴赏时详情面板需要重复展开、以及评论 emoji/stamp parity 缺口。仍存在：删除自己评论需要可逆端点确认后再做。下一优先级：刷新 Premium 搜索与远端屏蔽同步证据。 |
| Settings organization | 99% | Reworked Settings into native macOS Form groups for General, Reading, Discovery, Safety, Downloads, Account, and Runtime Readiness, added localized Settings search, moved the Settings search index terms into `SettingsSearchTerms.swift`, kept `SettingsView.swift` at 908 lines, and added repeatable `--visual-qa-settings-window` launch coverage that opens the native Settings scene directly. `settings-window` was recaptured at `20260525T174718Z` after the latest setting groups. | 已消失：设置项继续散落、查找下载/阅读/安全选项只能滚动、Settings 单文件超过 1000 行、设置窗口缺少视觉 QA surface、以及刷新设置截图需要手动打开 Settings 的风险。仍存在：新增设置分组后继续刷新 Settings 视觉证据。下一优先级：刷新签入 QA 矩阵。 |
| Discovery visual surfaces | 98% | `swift build`, `swift test`, and `./script/build_and_run.sh --verify` passed after adding shared `DiscoveryCardPresentation`, tightening trending-tag masonry spacing, replacing tag-card `GeometryReader` drawing with fixed container fill, giving Spotlight cards a stable native SwiftUI thumbnail/card model, and adding dedicated auto/two-column/three-column/compact gallery layout QA surfaces. Captured repeatable visual evidence for `gallery-feed`, `gallery-auto` at `20260525T152249Z`, `gallery-two-column` at `20260525T152308Z`, `gallery-three-column` at `20260525T152344Z`, `gallery-compact` at `20260525T152429Z`, `trending-tags`, `pixivision`, and `narrow-window`. | 已消失：热门标签卡片继续堆叠临时 Geometry 修补、Spotlight 卡片尺寸模型分散、Pixivision 文章内 Pixiv 标签/搜索链接缺少原生 resolver 接入、窄窗口状态缺少证据、以及自动/两列/三列/紧凑画廊模式缺少独立截图证据的风险。仍存在：可增加 Ugoira 独立媒体库截图作为后续媒体库扩展证据。 |
| Pixivision link audit | 96% | Added `PixivisionArticleLinkAuditor` to fetch the latest article HTML during Runtime Readiness, extract Pixiv/Pixivision URLs from detected links and `href`/`src` attributes, and verify they resolve through the native `PixivWebLinkResolver`. SwiftPM coverage checks artwork, user, tag, related Pixivision article, external-link filtering, and duplicate-link stability. `swift test` passed with 64 tests, and `KEIPIX_LIVE_PIXIVISION_AUDIT=1 swift test --filter PixivisionArticleLinkAuditTests/livePixivisionArticleAudit` verified public Pixivision article `10000` resolves live artwork and user links. | 已消失：Pixivision 只验证文章列表和静态 URL 样本、文章内真实链接是否还能原生路由缺少运行态证据的风险。仍存在：真实文章内容变动后需要在 signed-in Runtime Readiness 中周期性刷新证据。 |
| Gallery three-column density | 99% | Compared the pre-guest/test-account masonry path with current code: the true layout algorithm stayed shared, while Visual QA had local sample data that did not represent wide-heavy real feeds. Three-column fixed masonry now lowers the fixed-mode minimum column width and uses stricter dense span thresholds: ordinary, common wide, and common panoramic works through about 3.2 stay single-column, truly extra-wide works start spanning at 3.4, and only ultra-wide works at 4.0+ occupy the full row. Added model coverage for real-account-like wide aspect ratios, expanded `galleryLayoutArtworks` with 1.45-1.9 aspect-ratio samples, added `2.45..<3.4` common-panoramic coverage, and recaptured `gallery-three-column` at `20260525T180138Z` showing three visible lanes under the normal detail inspector layout. `swift test` passed with 67 tests. | 已消失：测试账号样本能三列但真实账号横图大量跨列后视觉退化成两列、三列模式在较窄内容栏过早降级、以及视觉 QA 样本过于理想的风险。仍存在：真实账号可继续抽查热门/关注 feed 的三列视觉密度。 |
| Visual QA evidence | 99% | Added `download-queue`, `downloaded-reader`, `reader-window`, `artwork-detail-social`, `settings-window`, `sharing-templates`, `muted-content`, `feedback-sheet`, `creator-profile`, `ugoira-player`, `batch-bookmark-preview`, `pixiv-link-drop`, `pixiv-id-open`, `manga-watchlist`, `series-sheet`, `cached-feed`, `ranking`, `gallery-auto`, `gallery-two-column`, `gallery-three-column`, and `gallery-compact` visual surfaces to the QA evidence model and matrix checks. Visual QA launch modes now force the local `Visual QA` sample session, skip Keychain bootstrap, and `./script/build_and_run.sh --verify` launches cached-feed sample mode so repeatable QA keeps the user's signed-in account unchanged. `ranking` was captured at `20260525T164052Z`, `gallery-three-column` at `20260525T180138Z`, `pixiv-id-open` at `20260525T182429Z`, `creator-profile` at `20260525T184155Z`, `feedback-sheet` at `20260525T183312Z`, `artwork-detail-social` at `20260525T191331Z`, `muted-content` at `20260525T171333Z`, `ugoira-player` at `20260525T172441Z`, `downloaded-reader` at `20260525T174017Z`, `settings-window` at `20260525T174718Z`, and `sharing-templates` at `20260525T185753Z`. | 已消失：下载页、下载后本地阅读器、阅读窗口、作品详情社交区、设置窗口、分享模板、屏蔽管理页、反馈屏蔽 sheet、作者 Profile、动图播放器、批量收藏预览、Pixiv 链接拖放入口、Pixiv ID 打开入口、漫画追更、系列 sheet、缓存恢复、排行榜和画廊布局只能靠口头视觉验收、阅读窗口会被主窗口面积抢占截图、QA 矩阵无法读取关键 UI 证据、以及自动验证流程误用真实登录态的风险。仍存在：后续新增 surface 时需要同步 manifest 覆盖。下一优先级：Premium/远端同步使用专门测试账号验收。 |
| Search | 99% | Added JSON import/export for the saved search library, covering filtered presets, saved keywords, and local history through native macOS open/save panels; `swift build`, `swift test` with 17 tests, and `./script/build_and_run.sh --verify` passed; Computer Use verified the Saved Searches UI exposes import/export beside the library filter. Earlier work added Pixiv Premium male/female popular sorts, 20k/50k/100k bookmark thresholds, non-premium sort fallback, Pixiv Web order mapping, search diagnostics, AI filters, local filtering, saved preset decoding, summaries, and Pixiv Web URLs. Current search sorting mirrors Pixes/Pixiv Web behavior: non-Premium users can choose limited popular preview through `/v1/search/popular-preview/illust`, while male/female full popular sorts stay Premium-gated with visible lock messaging. | 已消失：搜索过滤能力、热门男女排序、高收藏阈值、保存搜索/预设迁移、回归保护缺口、非 Premium 热门预览语义不清、以及 Premium 搜索诊断不可见风险。仍存在：完整男女热门排序的真实结果需要 Premium 测试账号验收。下一优先级：有 Premium 测试账号后运行 Settings → Runtime Readiness → Search Diagnostics。 |
| Ranking parity | 98% | Added a native SwiftUI ranking-mode menu to artwork and manga ranking feed headers, mirroring Pixes/Pixiv Web's same-page ranking mode switching while reusing existing `KeiPixStore.select(_:)` reload semantics. SwiftPM coverage checks that illustration and manga ranking route families stay aligned with sidebar sections. Added a dedicated `ranking` Visual QA launch mode and captured `ranking` at `20260525T164052Z`, covering the ranking mode menu, latest-date entry, dense three-column cards, selected artwork state, and detail inspector together. | 已消失：排行榜模式切换只能回侧边栏或发现页找入口、隐藏侧边栏后切榜路径过长、后续新增榜单路由容易漏进模式切换器、以及排行榜只能借普通画廊截图间接验收的风险。仍存在：真实账号排行榜可周期性补人工抽查，Premium/远端同步继续使用专门测试账号验收。 |
| Downloads | 99% | Added native SwiftUI page-range download sheet for multi-page artworks, queue support for arbitrary source page ranges, source-page labels in the download list, and localized English/Chinese strings. Added retry-all-failed backoff scheduling, an explicit Settings toggle for automatically bookmarking newly downloaded artworks, download-record-scoped local viewer resume, `download-queue` visual evidence, and a deterministic `downloaded-reader` launch mode that writes local PNG pages and opens the native downloaded artwork reader. `downloaded-reader` was captured at `20260525T174017Z`. | 已消失：50P+ 漫画只能整作或当前页下载、失败项批量重试瞬时压满连接、下载与收藏联动只能单向工作、下载页本地阅读器关闭后丢失页码、下载队列缺少视觉证据、以及本地下载阅读器缺少独立截图入口的风险。仍存在：真实下载库可继续做人工抽查。 |
| Reader | 99% | Added persisted default reading modes for ordinary artworks and manga/long multi-page works, shared by the detail inspector and standalone reader window, with Settings controls and localized copy. Added a standalone reader full-screen focus preset, floating focus controls, a page-jump overlay, per-artwork last-page resume, and downloaded local viewer resume backed by bounded local progress libraries. `swift build` and `swift test` with 26 tests passed. | 已消失：每次打开同类作品都回到固定阅读模式、50P+ 阅读器缺少快速跳页和专注全屏入口、多 P 作品重新打开回到第一页、以及本地下载阅读器不恢复页码的问题。仍存在：继续保留视觉 QA 和触控板回归验收。 |
| Ugoira | 98% | Added playback speed choices for 0.5x/1x/1.5x/2x, current-frame PNG export from the Ugoira player, a local generated-frame `ugoira-player` visual QA launch mode, QA matrix coverage, localized English/Chinese strings, and SwiftPM tests for delay scaling plus local visual sample packaging. `ugoira-player` was captured at `20260525T172441Z`. | 已消失：动图只能按原速播放、无法从 App 内快速导出当前帧、以及动图播放器缺少可重复视觉证据的风险。仍存在：媒体库独立入口成熟后可补封面/海报快捷操作。下一优先级：Premium/远端同步使用专门测试账号验收。 |
| AI visibility | 92% | `9e8004a` added Settings copy and Runtime Readiness row clarifying that KeiPix currently handles AI visibility locally and through search `search_ai_type` filters; remote Pixiv account-wide AI display remains outside the verified App API surface | 已消失：AI 显示能力边界不可见风险。仍存在：Pixiv account-wide AI display needs a verified App API endpoint before remote sync can be implemented. 下一优先级：keep local hide-AI and search AI filters as the supported path until an endpoint is verified. |
| Direct Pixiv navigation | 98% | `da812fa` added a native App Controls sheet for opening artwork or creator by numeric Pixiv ID, reusing existing signed-in artwork/user loading paths and adding SwiftPM input parsing tests; `121a70f` added Settings ID navigation diagnostics for the selected artwork and creator; `37dd4a4` added a read-only QA runner that includes ID diagnostics; `ac2b8c2` added search-field quick open prefixes such as `illust:123` and `user:123`; `e67cdd1` surfaced those hints in search and the ID sheet. Added `pixiv-id-open` visual QA launch coverage and captured the ID open sheet at `20260525T182429Z`, so URL drop and numeric ID entry now both have repeatable native surface evidence. | 已消失：只能从 Pixiv URL/clipboard 跳转、缺少 Pixez/Pixes 常见 ID 直达入口、ID 直达缺少可运行验收证据、以及 ID 快捷语法不可发现的风险。仍存在：真实账号的选中作品/作者 ID 诊断可周期性通过 Read-only QA 复核。下一优先级：Premium/远端同步使用专门测试账号验收。 |
| Store maintainability | 96% | Split artwork actions/download/follow helpers into `KeiPixStore+ArtworkActions.swift`, creator/comment/series/list API wrappers into `KeiPixStore+Social.swift`, and account bootstrap/login/logout/switching into `KeiPixStore+Accounts.swift`; `KeiPixStore.swift` is now 939 lines. `dfce604` moved download queue actions into `ArtworkDownloadStore+Queue.swift`, leaving `ArtworkDownloadStore.swift` at 630 lines. `swift test` passed with 67 tests after the account split. | 已消失：核心 Store 超 1000 行风险、账号会话逻辑继续堆在 feed Store 主文件里的风险。仍存在：download persistence and file inspection can still split further if export/delete features grow. 下一优先级：keep future account changes in `KeiPixStore+Accounts.swift`, and future download changes in queue, persistence, or file-inspection extensions by domain. |
| SwiftUI view maintainability | 97% | `3e260ea` split Gallery feed header, popular preview, grid, ranking date, and batch download popover; `e9c809d` split creator preview header, status banner, grid, load-more tile, and action menus; `45e60c1` split profile metrics, collection shortcuts, workspace/details, and related creators; `swift build` and `./script/build_and_run.sh --verify` passed | 已消失：`GalleryView.swift`, `UserPreviewListView.swift`, and `UserProfileSheet.swift` large-file maintenance risk. 仍存在：smaller profile header can still move later if follow/feedback grows. 下一优先级：keep new UI changes in dedicated subviews. |
| Signed-in runtime QA | 97% | `./script/build_and_run.sh --verify` launched the app, existing Keychain session loaded, feed/ranking/search/bookmarks/following creators/reader window/download queue/settings diagnostics were navigated through native UI, and network diagnostics passed Pixiv API plus image host checks. Latest Runtime Readiness run selected 2P artwork `#142077008` and copied P0 4/4, P1 8/8, P2 8/8 diagnostics with reader/detail/comment all passing. | 已消失：non-novel signed-in runtime evidence gap for primary routes, P0 QA execution surfaces, ID route checks, comment feedback readiness checks, and read-only QA needing multiple manual button runs. 仍存在：actual pass/fail evidence for Premium search and reversible remote writes depends on a Premium or disposable Pixiv test account. 下一优先级：Premium/远端同步使用合适账号单独验收。 |
| Mutable action QA readiness | 95% | `953d27b` added `MutableActionQAItem`, `MutableActionQAStatus`, Settings readiness UI, copyable QA checklist, diagnostics text, localization, and SwiftPM tests; `d2e489a` added a typed `TEST ACCOUNT` authorization sheet and reversible private bookmark add/remove plus private follow/unfollow QA runner; `swift test` passed 14 tests | 已消失：可变更动作验收状态不可见风险，以及收藏/关注 P0 缺少可执行测试账号流程的风险。仍存在：comment posting remains explicit-approval manual QA because the current Pixiv API surface has no reversible delete path in KeiPix. 下一优先级：use disposable account diagnostics for bookmark/follow, and keep comment posting as manual explicit-approval QA. |
| Feedback and local mute safety | 97% | `4f81689` added native `FeedbackReportSheet`, Pixiv-compatible safety reasons, report-summary copying, Pixiv Web handoff, and local mute entry points from artwork, comment, and creator menus; `94eb7be` added the profile-header creator feedback entry; `121a70f` added a comment feedback diagnostic; `37dd4a4` added a Settings comment feedback preview that opens the actual feedback sheet for the first loaded comment on the selected artwork. Added isolated `--visual-qa-feedback-sheet` launch mode, a local artwork feedback fixture, matrix coverage through the Safety/Filtering row, and captured `feedback-sheet` at `20260525T183312Z`. `swift test` passed with 71 tests. | 已消失：作品详情与作者 Profile 缺少安全反馈与屏蔽入口风险、评论反馈缺少可运行检查入口、评论反馈 sheet 难以从 Settings 做视觉验收、以及反馈/屏蔽 sheet 缺少可重复截图证据的风险。仍存在：remote report submission remains Pixiv Web handoff. 下一优先级：真实有评论作品上周期性运行 Preview Comment Feedback 并保存诊断。 |

## 2026-05-24

| Area | Progress | Evidence | Notes |
| --- | ---: | --- | --- |
| Native macOS route | 82% | SwiftPM app, SwiftUI views, AppKit bridges only for desktop integrations, persistent sidebar toolbar control, and in-app Pixivision article reading via native WebKit bridge | Continue keeping GPL reference projects as behavior references only. |
| Downloads | 90% | Added naming templates, current-page download actions, ugoira ZIP downloads, local ZIP preview, GIF export, queue filters, queue search, sorting, filtered failed-item bulk actions, visible-item bulk management, local storage totals, per-item file sizes, Pixiv link actions, and keyboard/Finder actions in local previews | Supports artwork ID, title, creator, page, extension, AI/R18/R18G, tag placeholders, metadata-backed ugoira items, status/type filtering, sorting, local queue management, downloaded library inspection, copied link sets, first visible reveal, confirmed visible-item deletion, local image reader shortcuts, current-page image sharing/copying/downloading, and ugoira preview shortcuts. |
| Search | 90% | Added autocomplete, local history, saved keywords, saved search presets, saved-search library search, advanced filters, active filter affordances, creator search, tag actions, tag Pixiv Web handoff links, visible result filter summaries, search-summary copying, loaded-result Pixiv link export, per-result Pixiv link copying, Pixiv Web search handoff links, saved-search Web handoff, and one-click filter reset | Saved presets restore keyword plus match type, sort, age, date, bookmarks, work type, and ugoira filters; saved and recent searches can be filtered, copied, promoted to current-filter presets, audited from the result header, recognized from the toolbar, opened in Pixiv Web, exported as Pixiv Web links from the search library, copied as individual or loaded artwork links from the result feed, and artwork detail tags can open or copy their Pixiv Web tag pages. |
| Creator workflows | 90% | Creator search/recommend/following/followers/related pages support local list search, follow/mute filters, native sort menus, resettable creator-list state, creator-list summary chips, confirmed bulk actions, follow visibility badges/checks, copied Pixiv profile links, visible creator link export, visible creator summary export, preview artwork context actions, artwork-detail creator quick actions, related-preview artwork selection, profile header Pixiv actions, profile relationship shortcuts, related-creator context actions, and corrected following/follower semantics | Creator flows now meet the 90% feature target; remaining work is runtime QA evidence across signed-in lists, relationship sheets, artwork-detail follow actions, and bulk actions. |
| Non-novel Pixiv client scope | 91% | Feed, masonry, reader, bookmarks, comments, history, filtering, downloads, muted content, ugoira playback/downloads/export, in-app Pixivision reader, reusable search presets, visible search-state actions, creator list management, artwork summary export, downloaded library management, low-disturbance app packaging verification, and a screenshot-safe runtime readiness diagnostics surface exist | Non-novel feature scope now meets the 90% target; remaining work should focus on signed-in runtime QA passes across feeds, detail, reader, creator lists, downloads, and account-sensitive flows before starting novels. |

Current policy: novels stay deferred until non-novel artwork, manga, discovery, account, download, and reader flows reach at least 90% with build and runtime evidence.
